rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /articles/{docId} {
      // Izinkan operasi get (baca dokumen tunggal) dan list (kueri koleksi)
      allow read: if true; 
      
      // Izinkan operasi create, update, delete
      allow write: if true; // 🚨 Untuk testing saja, batasi di produksi
    }
  }
}
